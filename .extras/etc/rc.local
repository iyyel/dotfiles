# Default rc.local for void; add your custom commands here.
#
# This is run by runit in stage 2 before the services are executed
# (see /etc/runit/2).

## cpupower
# Start the machine with the powersave CPU governor
cpupower frequency-set -g powersave > /dev/null

## microphone 
# Mute the microphone at startup
su iyyel -c 'amixer -q sset Capture nocap'
amixer -q sset Capture nocap
