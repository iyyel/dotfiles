#!/usr/bin/env zsh
#
# ~/.config/yabar/gpu
#
# Authors:
#   Iyyel <github.com/iyyel>
#

gpu() {
    gpu=`cat /proc/acpi/bbswitch | grep 'OFF'`
    
    if [[ -n $gpu ]]; then
        echo "GPU: OFF"
    else
        echo "GPU: ON"
    fi
}

gpu
