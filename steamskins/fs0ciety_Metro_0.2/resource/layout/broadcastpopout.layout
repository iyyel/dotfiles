"resource/layout/broadcastpopout.layout"
{
	layout
	{
		//Used as margin-top for the popout broadcast window.
		//It still creates a small white flickering window at the bottom.
		region { name=top y=42 }
		place { region=top control="BroadcastPage" }
	}
}